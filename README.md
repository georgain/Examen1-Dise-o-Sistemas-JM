Comandos GIT
git init
git checkout -b feature/tasks
git add .
git commit -m "feat: add tasks endpoint and DTOs"
git remote add origin <https://github.com/georgain/Examen1-Dise-o-Sistemas-JM>
git push -u origin feature/tasks

Comandos Docker
docker-compose up --build
docker-compose down
