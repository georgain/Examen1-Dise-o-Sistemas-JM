Comandos GIT
git init
git checkout -b feature/tasks
git add .
git commit -m "feat: add tasks endpoint and DTOs"
git remote add origin <URL>
git push -u origin feature/tasks

Comandos Docker
docker-compose up --build
docker-compose down
